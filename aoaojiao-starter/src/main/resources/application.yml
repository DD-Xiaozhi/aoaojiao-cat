server:
  port: 8001

spring:
  application:
    name: aoaojiao-o.o

  profiles:
    active: dev

mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: deleteFlag
      logic-delete-value: 1
      logic-not-delete-value: 0
